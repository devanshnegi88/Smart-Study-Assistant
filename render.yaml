services:
  - type: web
    name: smart-study-assistant
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python run:app
    env: python
    autoDeploy: true
    plan: free